"use client";
import {useRouter} from "next/navigation";import {useState} from "react";import {toast} from "sonner";import {supabase} from "@/lib/supabase";
export default function Reset(){const r=useRouter();const[password,setPassword]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const{error}=await supabase.auth.updateUser({password});if(error)toast.error(error.message);else{toast.success("Senha alterada.");r.push("/dashboard");}}return <main className="main" style={{maxWidth:420,margin:"0 auto"}}><form className="card grid" onSubmit={submit}><h1>Nova senha</h1><input className="input" type="password" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn">Salvar senha</button></form></main>}
