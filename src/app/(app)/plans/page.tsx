"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {supabase} from "@/lib/supabase";
export default function Plans(){const[plans,setPlans]=useState<any[]>([]);useEffect(()=>{supabase.from("subscription_plans").select("*").eq("is_active",true).order("price").then(({data})=>setPlans(data||[]))},[]);return <div className="grid"><h1>Escolha seu plano</h1><div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))"}}>{plans.map(p=><div className="card" key={p.id}><h2>{p.name}</h2><h3>{p.price===0?"Gratis":`R$ ${Number(p.price).toFixed(2).replace('.',',')}/mes`}</h3><p>{p.description}</p><p>{p.daily_limit??p.analysis_limit??"Ilimitadas"} analises/dia</p><Link className="btn" href={`/checkout/${p.id}`}>Assinar</Link></div>)}</div></div>}
